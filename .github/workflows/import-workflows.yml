name: Deploy n8n Workflows

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  import:
    runs-on: ubuntu-latest

    # ← Make secrets available to ALL steps
    env:
      N8N_API_URL: ${{ secrets.N8N_API_URL }}
      N8N_AUTH_TOKEN: ${{ secrets.N8N_AUTH_TOKEN }}

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install n8n CLI
        run: npm install -g n8n

      - name: Check n8n API health
        run: |
          echo "🔗 Pinging n8n API at $N8N_API_URL/rest/version"
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" "$N8N_API_URL/rest/version")
          if [ "$HTTP_STATUS" -ne 200 ]; then
            echo "❌ n8n API returned HTTP $HTTP_STATUS" >&2
            exit 1
          fi

      - name: Lint workflows JSON
        run: |
          echo "🔍 Validating workflows.ndjson"
          jq -s . workflows.ndjson > /dev/null

      - name: Import workflows into n8n
        run: |
          echo "🚀 Importing workflows.ndjson into $N8N_API_URL"
          if [ -f workflows.ndjson ]; then
            n8n import:workflow --input workflows.ndjson
          else
            echo "⚠️  No workflows.ndjson file found—skipping import"
          fi
